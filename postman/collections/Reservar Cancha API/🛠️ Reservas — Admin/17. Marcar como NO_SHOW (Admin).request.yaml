$kind: http-request
description: El equipo no se presentó. Cambia el estado a NO_SHOW.
url: "{{base_url}}/reservas/{{reserva_id}}/estado"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "estado": "NO_SHOW"
    }
order: 6000
